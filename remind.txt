
  （一）接口安全设计（课时8）：
  =======================================================
  username = read_panda
  password = md5(md5(123456)_pwd)
  timestamp = 15827048977

  token = md5(api_md5(read_panda)+md5(md5(md5(123456)_pwd))+md5(timestamp)_api)
  如果被篡改很容易发现：
  service_token=md5(api_md5(read_panda)+md5(md5(md5(1234567)_pwd))+md5(timestamp)_api)

（1）大量访问 time
（2）修改数据 token
（3）获取数据 加密敏感信息

  注意：如果有条件，强烈推荐https请求，更安全。参数没有过滤之前，不运行具体的接口。
  技巧：函数可以先调用，后定义。
          